import { Product } from "@/types/product";

interface CartItem {
  id: string;
  product: Product;
  quantity: number;
  variant: string;
  size?: string;
  price: number;
  discount?: number;
}

export interface CartState {
  items: CartItem[];
}
